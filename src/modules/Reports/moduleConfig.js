import { BarChart3 } from 'lucide-react';
import { reportTabs } from './data';

export const moduleConfig = {
  name: 'Reports',
  route: '/reports',
  icon: BarChart3,
  mockData: {
    reportTabs,
  },
};
